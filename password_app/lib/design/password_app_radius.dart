import 'package:flutter/material.dart';

class PasswordAppRadius {
  PasswordAppRadius._();

  static const Radius small = Radius.circular(12);
  static const Radius large = Radius.circular(20);
  static const Radius medium = Radius.circular(16);
}